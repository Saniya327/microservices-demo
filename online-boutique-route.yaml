apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: frontend-external
  name: frontend-external
  namespace: acm-online-boutique
  port:
    targetPort: 80
  to:
    kind: Service
    name: frontend-external
    weight: 100
  wildcardPolicy: None
